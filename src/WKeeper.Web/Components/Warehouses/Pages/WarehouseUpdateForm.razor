@inject IToastService ToastService

@inject IWarehouseService WarehouseService

@implements IDialogContentComponent<Warehouse>

<!-- Header -->
<FluentDialogHeader ShowDismiss="true">
    <FluentStack VerticalAlignment="VerticalAlignment.Center">
        <FluentIcon Value="@(new Icons.Regular.Size24.WindowApps())" />
        <FluentLabel Typo="Typography.PaneHeader">
            @Dialog.Instance.Parameters.Title
        </FluentLabel>
    </FluentStack>
</FluentDialogHeader>

<!-- Body -->
<FluentDialogBody>
    <EditForm EditContext="@_editContext" FormName="update-warehouse">
        <DataAnnotationsValidator />

        <FluentLabel Style="margin-bottom: 16px;">
            Asegurece de agregar los datos en el campo corecto
        </FluentLabel>

        <FluentTextField Name="update_name"
                         @bind-Value="@Content.Name"
                         Label="Name"
                         Required
                         Size="400" />
        <div style="color: var(--error);">
            <FluentValidationSummary />
        </div>
    </EditForm>
</FluentDialogBody>

<!-- Footer -->
<FluentDialogFooter>
    <FluentButton Appearance="Appearance.Accent"
                  Disabled="@(!_editContext.Validate())"
                  OnClick="@SaveAsync">
        Save
    </FluentButton>
    <FluentButton Appearance="Appearance.Neutral"
                  OnClick="@CancelAsync">
        Cancel
    </FluentButton>
</FluentDialogFooter>